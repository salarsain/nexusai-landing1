# URL of the deployed backend (no trailing slash, no /api suffix).
# Local dev default is http://localhost:5000 if this is left unset.
# In production (Vercel project settings) set this to your Railway URL, e.g.:
# VITE_API_URL=https://your-app.up.railway.app
VITE_API_URL=
